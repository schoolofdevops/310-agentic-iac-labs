# Task Contract

## Allowed tools

<the exact tool names/scopes this agent may use, e.g. `Read`, `Bash(terraform plan)`>

## Forbidden actions

<explicit actions this agent must never take, one per line>

## Required evidence

<what must exist before this task counts as done, e.g. "a real terraform plan
output, pasted here, showing 0 destroys">

## Stop condition

<what makes this task finished, e.g. "plan reviewed and approved by a human,
before any apply">
