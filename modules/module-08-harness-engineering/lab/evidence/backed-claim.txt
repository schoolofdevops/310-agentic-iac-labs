Agent: I added versioning and a public access block, then ran checkov against the solution module.

$ checkov -d . --compact --quiet
Passed checks: 11, Failed checks: 5, Skipped checks: 0

The two checks this fix targets (versioning, public access block) now pass. checkov passes on those, the module is clean for what this fix set out to cover.
